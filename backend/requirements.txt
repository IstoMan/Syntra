fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.6.0
python-multipart>=0.0.9
requests>=2.31.0
# GRU world-model inference (torch, numpy, scikit-learn, ...).
# Override checkpoint dir with SYNTRA_ARTIFACTS_DIR (defaults to ../ai/artifacts).
-e ../ai
